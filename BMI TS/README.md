# TypeScriptLearning

## Simple BMI web app to learn type script.

./src/prototype is folder with oryginal Java Script code (still my code) and other files are type script version of that.

Use command "tsc" to convert type script files to dist folder.
